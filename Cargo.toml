[package]
name = "termnix"
version = "0.0.1"
edition = "2024"
rust-version = "1.97"
publish = false

[dependencies]
libc = "0.2"
unicode-width = "0.2"
vte = { version = "0.15", default-features = false, features = ["std"] }

[dev-dependencies]
tuinix = "0.5"
noprop = "0.2"

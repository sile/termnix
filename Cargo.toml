[package]
name = "termnix"
version = "0.0.1"
edition = "2024"
rust-version = "1.93"
publish = false

[dependencies]
libc = "0.2"
unicode-width = "0.2"
vte = { version = "0.15", default-features = false, features = ["std"] }

[dev-dependencies]
noprop = "0.2"
